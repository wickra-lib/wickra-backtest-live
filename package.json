{
  "name": "wickra-backtest-live",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "backtest-live.wickra.org — in-browser backtesting demo + docs for the wickra-backtest engine, powered by wickra-backtest-wasm.",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.0",
    "vue": "^3.5.42",
    "wickra-backtest-wasm": "^0.1.7"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^6.0.3",
    "vite": "^8.3.0",
    "vite-plugin-wasm": "^3.6.0",
    "vue-tsc": "^3.3.11"
  }
}
